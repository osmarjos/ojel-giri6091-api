sevices:
  postgres-db:
    image: postgres
    container_name: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=ojel_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin123